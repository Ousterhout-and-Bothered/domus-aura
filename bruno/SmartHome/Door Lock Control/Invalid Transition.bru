meta {
  name: Invalid Transition
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/devices/{{doorLockId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "Lock"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
