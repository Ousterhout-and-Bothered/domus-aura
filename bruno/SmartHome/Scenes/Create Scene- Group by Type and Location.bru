meta {
  name: Create Scene- Group by Type and Location
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/scenes
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Living Room Cool",
    "actions": [
      {
        "deviceType": "Thermostat",
        "location": "Living Room",
        "operation": "SetDesiredTemperature",
        "value": "72"
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
