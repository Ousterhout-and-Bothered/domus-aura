meta {
  name: Turn Off
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/devices/{{lightId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setPower",
    "value": "Off"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
