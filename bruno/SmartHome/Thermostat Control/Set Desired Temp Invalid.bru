meta {
  name: Set Desired Temp Invalid
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/devices/{{thermostatId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setDesiredTemperature",
    "value": "hello"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
