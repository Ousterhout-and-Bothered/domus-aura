meta {
  name: Set Mode Heat
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/devices/{{thermostatId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setMode",
    "value": "Heat"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
