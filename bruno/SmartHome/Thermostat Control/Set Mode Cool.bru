meta {
  name: Set Mode Cool
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/devices/{{thermostatId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setMode",
    "value": "Cool"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
