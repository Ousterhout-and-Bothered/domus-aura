meta {
  name: Set Mode Auto
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/devices/{{thermostatId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setMode",
    "value": "Auto"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
