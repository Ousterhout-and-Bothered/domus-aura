meta {
  name: Set Ambient Temp
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/locations/{{locationName}}/ambient-temperature
  body: json
  auth: inherit
}

body:json {
  {
    "temperature": 70
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
