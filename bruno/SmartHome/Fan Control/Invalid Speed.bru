meta {
  name: Invalid Speed
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/devices/{{fanId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setSpeed",
    "value": 10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
