meta {
  name: Set High
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/devices/{{fanId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setSpeed",
    "value": "High"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
