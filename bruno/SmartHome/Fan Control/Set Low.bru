meta {
  name: Set Low
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/devices/{{fanId}}/state
  body: json
  auth: inherit
}

body:json {
  {
    "command": "setSpeed",
    "value": "Low"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
