meta {
  name: Turn off One Light
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "message": "Turn off hallway light."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
