meta {
  name: Set Desired Temperature All Rooms
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "message": "Change all thermostats to 70."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
