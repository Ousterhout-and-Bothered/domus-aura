meta {
  name: Set One Fan Speed
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "message": "Set living room fan to low."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
