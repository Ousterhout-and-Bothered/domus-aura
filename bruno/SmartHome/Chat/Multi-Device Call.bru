meta {
  name: Multi-Device Call
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "message": "Turn off all the lights and unlock the front door and lock the back door and set all thermostats to 68."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
