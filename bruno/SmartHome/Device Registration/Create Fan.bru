meta {
  name: Create Fan
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/devices
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Test Fan",
    "location": "Test Room",
    "type": 1
  }
}

tests {
  if (res.body?.id) {
    bru.setEnvVar("testDeviceId", res.body.id);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
